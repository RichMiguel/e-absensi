<% if (success_msg.length> 0) { %>
    <script>
        Swal.fire({
            toast: true,
            position: 'top-end',
            icon: "success",
            title: "<%= success_msg[0] %>",
            showConfirmButton: false,
            timer: 3000,
            timerProgressBar: true
        });
    </script>
<% } %>